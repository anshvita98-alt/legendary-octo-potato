import { config } from 'dotenv';
config();

// This is all you need to do to make your flows available in the developer UI
// Just import them.
import './flows/get-minting-assistance';
import './flows/generate-heirloom-story';
